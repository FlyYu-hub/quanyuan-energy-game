services:
  - type: web
    name: quanyuan-energy-game
    runtime: python
    plan: free
    buildCommand: pip install -r backend/requirements-render.txt
    startCommand: cd backend && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
